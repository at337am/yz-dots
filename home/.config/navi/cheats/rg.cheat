% grep, rg, search

# 搜索内容 (包含忽略, 不区分大小写, 包含隐藏)
rg --no-ignore -i.

# 只列出匹配的文件名 (--files-with-matches)
rg -l

# 每个匹配结果单独占一行输出 (--only-matching)
rg -o

# 指定查找深度为 1
rg --max-depth 1



% grep, rg, count

# 统计某个文件中, 匹配的个数
rg --no-ignore --hidden -o "xxx" [filename] | wc -l
