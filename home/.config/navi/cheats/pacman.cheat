% pacman, update

# 更新所有软件包
sudo pacman -Syu

# 更新所有软件包 (强制重新下载所有软件源数据库)
sudo pacman -Syyu



% pacman, clean

# 清理 - 删除旧的缓存文件 (保留当前版本的缓存)
sudo pacman -Sc --noconfirm

# 清理 - 删除所有缓存文件
sudo pacman -Scc --noconfirm



% pacman, install

# 安装软件
sudo pacman -S xxx



% pacman, remove

# 删除软件包以及依赖
sudo pacman -Rns xxx



% pacman, find

# 同步文件数据库
sudo pacman -Fy

# 查找某个命令属于哪个包
pacman -F xxx
